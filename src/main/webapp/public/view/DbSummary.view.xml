<mvc:View
        xmlns:mvc="sap.ui.core.mvc"
        xmlns="sap.m">
    <Page showHeader="false">
        <List headerText="Database Summary" inset="false"
              items="{/dbsummary}">
            <ObjectListItem
                    intro="{name}"
                    title="{mnemonic}" number="Number of rows: {count}"
            />
        </List>
    </Page>
</mvc:View>